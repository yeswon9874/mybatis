<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yeswon.pj.mapper.TestMapper">
<!--    <select id="selectTest"  parameterType="java.lang.Long" resultType="hashmap">-->
<!--        select SYSDATE();-->
<!--    </select>-->

    <select id="selectBoardList" resultType="com.yeswon.pj.domain.BoardDto">
        SELECT
            seq, mb_id, mb_pw, address, mb_tell
        FROM
            member
		<if test = 'seq != null'>
			where seq = #{seq}
		</if>
    </select>

    <insert id="insertBoard" parameterType="com.yeswon.pj.domain.BoardDto">
			INSERT INTO member
			(
				mb_id, mb_pw, address, mb_tell
			)
			VALUES
			(
				#{mbId},
				#{mbPw},
				#{address},
				#{mbTell}
			)
    </insert>

	<update id="updateMember" parameterType="com.yeswon.pj.domain.BoardDto">
		UPDATE member
		SET mb_pw = #{mbPw},
		address = #{address},
		mb_tell = #{mbTell},
		mb_id = #{mbId}
		WHERE seq = #{seq}
	</update>
</mapper>
